#include<stdio.h>
main()
{
/* the program is about to copy one array to another array*/
	int i,a[3],b[3];
	printf("Enter the number to first array:\n");
	for(i=0;i<3;++i)
	{
		scanf("%d",&a[i]);
		scanf("%d",&b[i]);
	}
	f
}
