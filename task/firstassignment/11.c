#include<stdio.h>
main()
{ 
  int length,breadth,S;
  printf("Enter the value to length");
  scanf("%d",&length);
  printf("Enter the number to breadth");
  scanf("%d",&breadth);
  S=length*breadth;
  printf("The area of rectangle is %d",S); 
}
